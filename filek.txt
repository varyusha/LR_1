Збережений у коміті (commited), змінений (modified) та індексований (staged).
Приймає шлях файлу або директорії.
Показує різні стани файлів у робочій директорії та індексі.
Представляє той момент часу, коли користувач вже вніс необхідні зміни як одну логічну одиницю та готовий до збереження (якщо немає помилок).
Ініціалізація гіту у папці проекту.
Потрібно писати короткі зміни, опис дій (виключно англійською).
Використовується для переключення гілок та отримання вмісту до робочої директорії.
Використовується для скасування певної дії.
Використовується, щоб показати записану історію проекту, починаючи з останнього відбитку коміту у зворотному порядку.
Показує різні стани файлів у робочій директорії та індексі.
Команда, що відновлює файли з попередньо створеного коміта.
git checkout "name commit" | git checkout -b "ім'я нової гілки" "name commit".
git branch "назва гілки".
git checkout "назва гілки" | git switch "назва гілки".
git branch master/main.
git merge "приєднувана_гілка".
